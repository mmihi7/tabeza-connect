{
  "name": "@tabeza/virtual-printer",
  "version": "1.0.0",
  "description": "TABEZA Virtual Printer Interface - POS-agnostic receipt capture and processing",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint src --ext .ts,.tsx"
  },
  "keywords": [
    "pos",
    "printer",
    "receipt",
    "escpos",
    "africa",
    "etims",
    "kra"
  ],
  "dependencies": {
    "@tabeza/receipt-schema": "workspace:*",
    "node-escpos": "^0.0.3",
    "pdf-parse": "^1.1.1",
    "sharp": "^0.33.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "eslint": "^8.0.0"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node"
  }
}